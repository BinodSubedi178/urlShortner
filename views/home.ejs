<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Url Shortener Service</title>
    <style>
        body{
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        h1{
            text-align: center;
            color: #333;
        }
        form{
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }
        input{
            margin: 10px;
            font-size: 1rem;
            padding: 10px;
            width: 300px;
            border: 1px solid #333;
            border-radius: 5px;
        }
        button{
            padding: 10px;
            border: 1px solid #333;
            border-radius: 5px;
            background-color: #333;
            color: #fff;
            cursor: pointer;
            margin: 10px;
        }
        button:hover{
            background-color: #555;
        }
        table{
            width: 100%;
            border-collapse: collapse;
        }
        th, td{
            border: 1px solid #333;
            padding: 10px;
            text-align: center;
        }
        th{
            background-color: #333;
            color: #fff;
        }
        tr:nth-child(even){
            background-color: #f2f2f2;
        }

    </style>
</head>
<body>
    <h1>Url Shortner Service</h1>
    <div>
    <form method = "POST" action="/url">
        <label>
            Enter Your Original Url Here:
        </label>
        <input type="text" name= "url" placeholder="https://example.com">
        <% if(locals.id){ %>
         <p>Your Short url is:<em>http://localhost:3001/url/<%= id %></em>  </p>
            <% } %>
        <button type="submit">Generate</button>
        <% if(locals.urls){ %>
            <table>
                <thead>
                    <th>S.N</th>
                    <th>Url</th>
                    <th>Short-Id</th>
                    <th>Clicks</th>
                </thead>
                <tbody>
                    <% urls.forEach((url, index) => { %>
                        <tr>
                        <td> <%= index + 1 %> </td>
                        <td> <%= url.redirectUrl %> </td>
                        <td><%= url.shortID %></td>
                        <td><%= url.visitHistory.length%></td>
                        <tr>
                    <% }) %>
                </tbody>
                <% } %>
    </form>
</div>
</body>
</html>